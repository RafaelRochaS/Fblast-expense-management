apiVersion: v1
kind: Service
metadata:
  name: {{ include "exp-mgmt-chart.fullname" . }}
  labels:
    {{- include "exp-mgmt-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exp-mgmt-chart.selectorLabels" . | nindent 4 }}
